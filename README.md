# TransprintBack

Node 22.18.0 (LTS)

inicio de docker

con docker instalado

docker-compose up -d
docker ps -> chequeas que este creado
docker exec -it mydb_container mysql -uuser -ppass mydb -> abro una consola mysql